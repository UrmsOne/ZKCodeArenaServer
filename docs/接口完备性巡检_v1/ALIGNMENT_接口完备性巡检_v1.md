## 项目上下文
- **项目类型**：Go 语言后端服务，主模块为 `pkg/app/api-server`
- **关键依赖**：MongoDB、Redis、Viper 配置、服务层封装（`service_*`）
- **接口层**：REST API 统一在 `pkg/app/api-server/server` 目录注册
- **响应规范**：`pkg/utils/response.go` 定义标准响应包装

## 原始需求
- 校验 `server` 层所有 REST API 是否覆盖 `docs/v1.0需求/v1.0需求文档.md`
- 确认每个接口的响应结构、权限校验符合需求
- 通过最佳实践方式集成 Swagger/OpenAPI 生成接口文档
- 构建覆盖接口完整链路的单元/集成测试，确保“代码正确性以单元/集成测试覆盖为主”
- 交付可供前端使用的完整接口文档

## 任务边界
- 仅针对 `pkg/app/api-server/server` 下暴露的 REST API
- 以 `v1.0` 需求文档为验收基准；无额外业务扩展
- 测试聚焦在 handler→service→数据层的业务流程，可通过 mock 实现外部依赖
- 验收环境：开发环境；无需部署生产或 CI

## 需求理解
- **覆盖范围**：课程、班级、任务、题库、判题、提交、统计、用户等模块的所有 handler
- **合规校验**：逐条对照需求文档确认资源、权限、状态机、异常返回
- **测试策略**：以 Go testing 为基础，结合 httptest/Mock 实现链路级验证
- **文档策略**：优先采用 `swaggo/swag` 生态（成熟、与 Gin 兼容）生成 Swagger 文档

## 已解答问题
- 任务命名：`接口完备性巡检_v1`
- 测试范围：需保证整条链路打通
- Swagger 方案：由我评估后选定最合适实现（默认 swaggo）
- 验收基准：需求文档 + 单元/集成测试通过，开发环境验证

## 风险与假设
- 假设需求文档是最新版本；若发现冲突需回报
- 假设现有数据库模型与接口一致，无大规模 schema 变更
- Swagger 生成可能需调整现有 Router/模型标签

## 后续行动
1. 梳理接口清单与需求映射，输出共识文档
2. 制定设计方案（测试策略、Swagger 集成、自动化流程）
3. 拆分原子任务并进入实现阶段

