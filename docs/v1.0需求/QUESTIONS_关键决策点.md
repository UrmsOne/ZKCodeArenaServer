# 关键决策点 - 需要确认

## 📋 文档说明
本文档列出了在分析 v1.0 需求和现有代码后，需要人工确认的关键决策点。这些决策将影响后续的开发方向和工作量。

**创建时间**: 2025-10-06  
**优先级排序**: 高 → 中 → 低

---

## 🔴 高优先级决策（影响核心功能）

### Q1: 密码安全问题
**问题描述**:  
在 `service_user.go` 第118-125行，代码中已实现密码哈希生成，但在验证时却使用明文比较：
```go
// 已生成哈希
hashedPassword, err := bcrypt.GenerateFromPassword([]byte(user.Password), bcrypt.DefaultCost)

// 但验证时是明文比较（第123行）
if user.Password != password {
    return nil, errors.New("账号或密码错误")
}
```

**决策选项**:
1. ✅ **推荐**: 修复代码，正确使用 bcrypt 验证
   - 影响：需要重置所有现有用户密码
   - 工作量：小（代码修改简单）
   - 安全性：高

2. ❌ 保持现状（不推荐）
   - 风险：密码泄露风险极高

**您的决定**:  选项1
□ 选项1 - 修复密码加密（推荐）  
□ 选项2 - 暂不修复  
□ 其他方案: _______________

---

### Q2: 校外学习者角色设计
**问题描述**:  
需求文档提到三种注册类型：
1. 学生注册 - 需要学号、关联班级
2. 教师注册 - 需要工号、审核
3. 校外学习者注册 - 姓名、手机号、无需关联班级

但代码中角色类型只有：`admin`, `teacher`, `student`, `contest`

**决策选项**: 
1. 新增 `learner` 角色
   - User模型新增字段区分学生类型
   - 工作量：中

2. 使用 `student` 角色 + `is_external` 布尔字段
   - 不改动角色枚举
   - 工作量：小

3. 统一作为 `student` 角色
   - 不区分内外部学生
   - 工作量：无

**您的决定**:  3.不区分
□ 选项1 - 新增learner角色  
□ 选项2 - 使用is_external字段（推荐）  
□ 选项3 - 不区分  
□ 其他方案: _______________

**补充说明**: _________________

---

### Q3: 教师审核流程设计
**问题描述**:  
需求要求"教师注册后进入管理员审核队列"，但当前实现是直接创建用户，没有审核机制。

**需要确认**:
1. 审核状态存储位置？
   - User模型添加字段: `approval_status`, `approved_by`, `approved_at`
   
2. 审核流程？
   - 教师注册 → `approval_status = pending` + `is_active = false`
   - 管理员审核 → 通过/拒绝
   - 通知方式？（邮件/站内信）

3. 审核权限？
   - 只有 admin 可以审核？
   - 还是特定的"审核员"角色？

**您的决定**:  暂不实现审核功能
□ 按上述方案实现（User模型添加字段）  
□ 使用独立的审核表  
□ 暂不实现审核功能  
□ 其他方案: _______________

**具体方案**: _________________

---

### Q4: 班级与课程的关系
**问题描述**:  
需求文档的描述和数据模型设计存在表述差异：

**需求文档说**: "课程创建时选择关联已创建班级"  
**数据模型是**: `Clazz.CourseId` (班级属于课程)

**当前实现**: 先创建课程，再创建班级时指定课程ID ✅ 这是正确的

**需要确认的问题**:
1. 一个课程可以有多个班级？ ✅ 是（已实现）
2. 一个班级只能属于一个课程？ ✅ 是（已实现）
3. 是否需要"班级池"，创建课程时从池中选择班级？
   - 目前实现：课程创建后，再为该课程创建班级
   - 需求可能想要：先有班级，创建课程时关联

**您的决定**:  保持当前实现
□ 保持当前实现（课程 → 班级的创建顺序）  
□ 改为：班级可以独立存在，课程创建时关联  
□ 两种方式都支持  

**说明**: _________________

---

### Q5: 任务完成标记逻辑
**问题描述**:  
代码中有占位符接口 `FinishTask` (server_course.go:210-215)，但逻辑未实现。

**需要明确**:
1. **完成条件**： 
   - □ 提交所有题目？
   - □ 至少通过N道题？
   - □ 所有题目都AC？
   - □ 其他: _______________

2. **完成后的处理**：
   - □ 只是标记完成状态？
   - □ 计算成绩/积分？
   - □ 解锁后续任务？
   - □ 通知教师？

3. **数据存储**：
   - Task模型中有 `FinishNums` 字段
   - 但没有记录"哪些学生完成了"的字段
   - 是否需要新的 `task_completion` 表？

**您的决定**:  暂不实现
□ 创建独立的 TaskCompletion 表  
□ 在 Task 中存储完成学生ID列表  
□ 通过 Submit 记录反向查询  
□ 其他方案: _______________

**完成条件**: _________________

---

## 🟡 中优先级决策（影响用户体验）

### Q6: 短信验证码服务商选择
**问题描述**:  
密码找回功能需要手机验证码，需要选择服务商。

**常见选项**:
1. 阿里云短信服务
2. 腾讯云短信服务
3. 网易云信
4. 暂不实现密码找回功能

**需要确认**:
- 是否已有账号？供应商: _______________
- API密钥是否准备好？
- 预算：___ 元/月
- 验证码模板是否已申请？

**您的决定**:  暂不实现
□ 使用阿里云（请提供密钥）  
□ 使用腾讯云（请提供密钥）  
□ 暂不实现  
□ 其他: _______________

---

### Q7: Course模型的"学期"字段
**问题描述**:  
需求文档提到创建课程时填写"学期"，但 Course 模型没有该字段。

**影响**: 无法按学期筛选和管理课程。

**决策选项**:
1. 添加 `semester` 字符串字段（如："2024-2025学年第一学期"）
2. 添加结构化字段：`year`(int) + `term`(int)
3. 不添加，使用课程描述字段

**您的决定**:  不添加
□ 选项1 - semester字符串  
□ 选项2 - 结构化字段  
□ 选项3 - 不添加  

---

### Q8: 任务类型 - 视频资源管理
**问题描述**:  
Task模型有 `Type` 字段，支持：
- `TaskTypeProblem = 1` (题单) ✅
- `TaskTypeVideo = 2` (视频) ❌ 未实现

**需要确认**:
1. 视频存储方式？
   - □ 本地存储
   - □ 云存储（OSS/COS）
   - □ 第三方视频平台（B站/腾讯视频）
   
2. 视频管理？
   - 是否需要独立的 `Video` 模型？
   - 还是直接在 Task 中存储视频URL？

3. 优先级？
   - v1.0 是否必须实现视频功能？

**您的决定**:  v1.0 不实现视频功能
□ v1.0 不实现视频功能（建议）  
□ 使用云存储 + Video模型  
□ 只存储第三方平台链接  
□ 其他: _______________

---

### Q9: 代码运行测试（非提交）
**问题描述**:  
需求要求"代码运行测试：使用当前代码运行可见测试用例，展示执行结果"，但当前只有提交判题功能。

**差异**:
| 功能 | 代码运行测试 | 提交判题 |
|-----|------------|---------|
| 用例范围 | 仅示例用例 | 全部用例 |
| 计入记录 | 否 | 是 |
| 更新统计 | 否 | 是 |
| 用途 | 调试 | 正式提交 |

**需要确认**:
1. 是否要实现运行测试接口？
2. 是否限制调用频率？（防止滥用判题资源）
3. 是否允许自定义输入？

**您的决定**:  实现运行测试接口(下游接口适配的话就复用)
□ 实现运行测试接口（推荐）  
□ 暂不实现  
□ 其他限制: _______________

---

### Q10: 题目搜索功能
**问题描述**:  
需求要求"题库搜索：支持题目标题、描述模糊搜索"，但当前只实现了筛选（难度、标签）。

**实现方式**:
1. MongoDB文本搜索（简单但功能有限）
2. Elasticsearch（功能强大但增加复杂度）
3. 简单的正则匹配

**需要确认**:
- 搜索准确度要求？
- 是否需要高亮显示？
- 是否需要搜索历史？

**您的决定**:  MongoDB $regex 搜索 支持题目标题、描述模糊搜索 难度 标签搜索
□ MongoDB $regex 搜索（推荐）  
□ 使用 Elasticsearch  
□ 暂不实现  

---

## 🟢 低优先级决策（可延后）

### Q11: 接口速率限制
**问题描述**:  
当前没有接口访问频率限制，可能被恶意请求攻击。

**建议**: 使用 Redis 实现令牌桶/滑动窗口限流。

**您的决定**:  延后到v1.0实现
□ v1.0 实现  
□ 延后到 v1.1  

---

### Q12: 题目代码模板
**问题描述**:  
不同语言的题目需要初始代码模板。

**实现方式**:
1. Problem模型添加 `templates` map字段
2. 独立的 Template 表
3. 前端硬编码

**您的决定**:  
□ Problem模型添加字段  
□ 独立表  
□ 延后实现  

---

### Q13: 日志和监控
**问题描述**:  
当前有logrus日志，但缺少：
- 性能监控
- 错误追踪（如Sentry）
- 请求追踪

**您的决定**:  延后实现
□ v1.0 集成Sentry  
□ 延后实现  
□ 不需要  

---

## 📊 决策汇总表

| 编号 | 问题 | 优先级 | 是否阻塞开发 | 您的决定 |
|-----|------|--------|------------|---------|
| Q1 | 密码安全 | 🔴高 | 是 | _________ |
| Q2 | 校外学习者角色 | 🔴高 | 是 | _________ |
| Q3 | 教师审核流程 | 🔴高 | 是 | _________ |
| Q4 | 班级课程关系 | 🔴高 | 否 | _________ |
| Q5 | 任务完成逻辑 | 🔴高 | 是 | _________ |
| Q6 | 短信服务商 | 🟡中 | 部分 | _________ |
| Q7 | 学期字段 | 🟡中 | 否 | _________ |
| Q8 | 视频资源 | 🟡中 | 否 | _________ |
| Q9 | 代码运行测试 | 🟡中 | 否 | _________ |
| Q10 | 题目搜索 | 🟡中 | 否 | _________ |
| Q11 | 速率限制 | 🟢低 | 否 | _________ |
| Q12 | 代码模板 | 🟢低 | 否 | _________ |
| Q13 | 监控系统 | 🟢低 | 否 | _________ |

---

## ✅ 推荐方案（基于行业经验）

如果您希望快速决策，以下是我基于行业最佳实践的推荐方案：

### 立即执行（高优先级）
1. ✅ **Q1 密码安全** → 选项1：修复bcrypt验证
2. ✅ **Q2 校外学习者** → 选项2：添加`is_external`字段
3. ✅ **Q3 教师审核** → User模型添加审核字段，实现审核接口
4. ✅ **Q4 班级课程** → 保持当前实现，补充查询接口
5. ✅ **Q5 任务完成** → 创建TaskCompletion独立表

### 短期规划（中优先级）
6. ✅ **Q6 短信服务** → 延后到v1.1（可先实现管理员重置密码）
7. ✅ **Q7 学期字段** → 添加semester字符串字段
8. ✅ **Q8 视频资源** → v1.0不实现
9. ✅ **Q9 代码运行** → 实现简单版本（仅运行示例用例）
10. ✅ **Q10 题目搜索** → MongoDB $regex 搜索

### 延后处理（低优先级）
11. ⏸️ **Q11-Q13** → 延后到v1.1或v1.2

---

## 📝 反馈方式

请通过以下方式反馈您的决策：

1. **直接在本文档中勾选/填写** ✅ 推荐
2. **回复文字说明**（如："Q1选方案1，Q2选方案2..."）
3. **语音/会议讨论**（我会记录并更新文档）

**期望反馈时间**: 24小时内（以便继续后续工作）

---

**文档创建者**: AI Assistant  
**创建时间**: 2025-10-06  
**状态**: ⏳ 等待反馈

